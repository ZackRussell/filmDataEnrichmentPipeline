# filmDataEnrichmentPipeline
A schema-agnostic ETL tool that enriches any movie dataset (CSV or SQLite) with TMDB data, using fuzzy title matching and confidence-scored, non-destructive date disambiguation.
